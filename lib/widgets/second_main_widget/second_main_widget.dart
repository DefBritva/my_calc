import 'package:flutter/material.dart';

class SecondMainWidget extends StatelessWidget {
  const SecondMainWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
